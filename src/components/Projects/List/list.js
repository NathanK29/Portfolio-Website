import React from "react";
import "./list.scss";

const list = () => {
    // projects = [];
    // for (project in )

  return (
    <div className="greetingContainer">
      <div className="titleMain">PROJECTS</div>
      
    </div>
  );
};

export default list;
